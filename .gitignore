# Go build artifacts (see justfile: build)
bin/

# Runtime data: SQLite store + sidecars (WAL/SHM, capture JSONL, secrets store)
gateway.db*

# run-test state (scripts/run-test.sh): ports, generated config, test store
.run-test/

# Downloaded model pricing data (not committed)
internal/pricing/models.json

# Local gateway config (see gateway.toml.example)
gateway.toml

# Logs and editor junk
*.log
.DS_Store
*.tmp
.env